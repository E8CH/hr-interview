# 작업 이력 · 이어서 하기 (MEMORY)

다른 PC 에서 `git clone` · `git pull` 로 이 저장소를 받아 바이브코딩을 이어갈 때
**가장 먼저 읽는 문서**다. 코드나 커밋 로그만 봐서는 알 수 없는 것 — 왜 그렇게
만들었는지, 어디에 함정이 있는지, 지금 어디까지 왔는지 — 만 적는다.
(코드 구조 설명은 `docs/`·`bmad/`·각 서비스 README 에 있다.)

최종 갱신: 2026-08-01

---

## 1. 이 시스템이 무엇인가

LG 해커톤 과제 — HR 면접 일정 자동 생성. FastAPI 마이크로서비스 7개와
Streamlit 콘솔 1개로 되어 있다.

| 포트 | 서비스 |
|------|--------|
| 8001 | version-manager |
| 8002 | distributor |
| 8003 | response-collector |
| 8004 | scheduler |
| 8005 | repair-engine |
| 8006 | notification-hub |
| 8007 | audit-analytics |
| 8501 | 콘솔 (`tools/test_console.py`) |

로컬은 Windows/PowerShell (`start_all.ps1`), 배포는 Railway 의 리눅스 컨테이너
하나에 전부 띄우고 **8080 포트로 콘솔만** 노출한다.

## 2. 업무 흐름 (이게 제품의 뼈대다)

```
인사 ① 지원자 명단 올리기
인사 ② 팀별 명단 나누기 + 면접 차례 잡기 (면접 진행 조건도 여기서 정한다)
부서 ① 우리 팀 면접 담당자 정하기
인사 ③ 각 팀에 명단 보내기            → data/handoff/<round>.json
부서 ② 면접자–담당자 매칭 후 인사에 제출
인사 ④ 면접 시간표 만들기              → 04-scheduler
```

팀 구분은 **인사 ① 의 취합파일 `담당팀` 칸** 에서 시작한다 (3 절 참고).
인사 ② 는 그걸 승계할지 버리고 다시 나눌지만 고른다.

**뒷 단계는 앞 단계에 종속된다.** 특히 ④ 시간표는 부서가 ② 에서 짝지어 보낸
사람만 대상으로 만든다. 이 종속을 끊으면 화면 안에서 숫자가 서로 어긋난다
(실제로 그랬다 — 4 절 참고).

## 3. 코드만 봐서는 모르는 것들

### 콘솔 ↔ 스케줄러 연결은 서비스가 아니라 파일이다
`data/handoff/<round_id>.json` 한 장이 인사 화면과 부서 화면의 유일한 접점이다.
서비스 API 가 아니다. 구조:

```jsonc
{
  "round_id": "...", "plan_id": "...", "sent_at": "...", "sent_by": "...",
  "order":   { "<팀>": [{applicant_id, name, order, day, time}, ...] },  // 2단계 차례
  "order_at": "...",
  "timing":  { "start": "09:00", "minutes": 30, "rest": 5, "per_day": 8 }, // 2단계 진행 조건
  "teams": {
    "<팀>": {
      "applicants":    [{applicant_id, name, degree_type, major_final,
                         order, order_day, order_time}, ...],
      "order_planned": true,          // 2단계 차례를 그대로 쓴 경우에만 true
      "interviewers":  [...],
      "draft":     { pairs: {...} },  // 부서가 배정만 한 상태
      "submitted": {                  // 인사에게 제출한 확정본 ← 시간표의 입력
        pairs: {...},                 //   {지원자: 담당자}
        seats: { "<지원자>": {day: 1, slot: 0} }  // 부서 ③ 시간표에서의 자리
      }
    }
  }
}
```

`handoff_pairs()` 는 **`submitted.pairs` 만** 모은다. `draft` 는 시간표에 가지 않는다.
`handoff_seats_by_team()` 이 `submitted.seats` 를 모아 최종 시간표에 넘긴다 —
**몇 일차 · 몇 번째 칸**이지 달력 날짜가 아니다. 부서 화면은 그 날이 달력의
언제인지를 모르고(그건 인사팀이 정한다) 하루 몇 칸씩 끊어 세기만 하기 때문이다. 옛
회차에는 `seats` 가 없다 — 그때는 예전처럼 시각을 새로 짠다.

### 스케줄러의 시간 모델은 '자리 번호' 다, 시각이 아니다
`shared/contracts/constants.py` 의 고정 계약이다.

```python
DAYS  = ["1일차", "2일차", "3일차", "4일차", "5일차"]   # 달력 날짜가 아니라 며칟날
HOURS = ["1타임", "2타임", ..., "8타임"]        # 하루 8칸 — 점심을 따로 두지 않는다
DEFAULT_TIMING = {"start": "09:00", "minutes": 30, "rest": 5}
FRONT_END_MINUTES  = 14 * 60                    # 앞타임 — 아침부터 이 시각까지
BACK_START_MINUTES = 12 * 60                    # 뒤타임 — 이 시각부터 끝까지
MAX_SLOTS_PER_DAY = len(HOURS)                  # board.py
```

칸 이름은 **자리 번호다.** 여기에 "13시" 처럼 시각을 붙이면 진행 조건이 바뀌는
순간 이름이 거짓말을 한다 (30분 · 쉼 5분이면 13시라고 적힌 칸이 실제로는
11시 20분이다). 실제 몇 시 몇 분인지는 2단계에서 정한 `timing` 이 정하고,
`slot_spans(timing)` 하나가 그 계산을 도맡는다.

가능 시간은 **저장된 상수가 아니라 계산 결과다.** `hour_bands(칸, timing)` 이
그 칸의 실제 시각을 두 기준과 견줘 정한다.

- 끝 시각이 14시 이전 → **앞타임** 칸
- 시작 시각이 12시 이후 → **뒤타임** 칸
- 12시 ~ 14시 사이의 칸은 **양쪽 다** 나온다 (그래서 함수 이름이 복수형이다)

두 덩어리는 **일부러 겹쳐 뒀다.** 예전에는 정오를 기준으로 오전 · 오후로
**갈랐고**, 그래서 정오에 걸치는 칸(기본 조건 6타임 11:55~12:25)을 어느 쪽도
못 맡아 점심때가 통째로 비었다. 겹쳐 두면 `FRONT_HOURS ∪ BACK_HOURS == HOURS`
라 그 구멍이 구조적으로 안 생긴다. 다만 **겹치므로 합집합은 중복이 있다** —
전체를 뜻할 때는 여전히 `HOURS` 를 쓴다.

기본 조건에서는 마지막 칸이 13시 35분에 끝나 **앞타임이 하루 전체**이고,
실제로 자리를 좁히는 쪽은 **뒤타임(7·8타임)** 뿐이다. 그래서 제약이 걸리는지
보는 시험은 뒤타임으로 써야 한다. 되읽을 때(`band_of`)도 하루 전체를 덮으면
`둘 다` 로 나온다 — 구별할 수가 없고, 제약이 없다는 뜻이 맞다.

덩어리 이름은 **DB 에 남지 않는다.** `availability` (칸 목록)만 저장하고 이름은
`band_of()` 로 되읽는다. 그래서 이번처럼 표기를 갈아도 마이그레이션이 없다.
들어오는 쪽(엑셀 `가능시간` 칸 · `PUT /interviewers/bands`)은 `band_name()` ·
`LEGACY_BANDS` 로 옛 표기(`오전만` · `오후만`)를 그대로 받아 준다.

→ 한 팀이 하루에 넣을 수 있는 인원은 최대 8명 (규칙2가 `(팀, 날, 칸)` 을
하나로 묶으므로 팀당 하루 상한은 곧 `len(HOURS)` 다).

**옛 자료:** 이름을 바꾸기 전에 저장된 DB 에는 `{<아무 날>: ["09시", ...]}` 처럼
옛 칸 이름이 남아 있다. 그대로 읽으면 어느 칸에도 안 걸려 그 사람이 통째로
빠지고 배정이 0명이 된다(날 이름 쪽은 무엇이든 어차피 지워진다). `normalize_availability()` 가 읽는 자리(`schedule_service.row_to_interviewer`
· `_merge_availability` · `api/interviewers._serialize` · `response_client`)에서
그 이름이 뜻하던 앞/뒤만 살려 지금 칸으로 옮긴다. 시각을 그대로 믿지는
않는다 — 진행 조건이 바뀌면 그 시각은 이미 다른 칸이기 때문이다.

### 하드 제약은 `Board` 한 곳에만 있다
`services/04-scheduler/app/services/board.py`. v1·v4·v5 알고리즘은 전부
`Board.place()` 로만 배치하므로, **제약이나 고정 짝을 손볼 때 알고리즘은 건드릴
필요가 없다.** v5 = v4 에 `days_per_team=3, fallback=True`.

### 팀 나눔은 취합파일의 `담당팀` 에서 시작해 배정 결과로만 흐른다
1단계 병합이 취합본에 **`담당팀`** 컬럼을 새긴다 (`merge_service.TEAM_HEADER`).
값은 `희망지원자_{팀}` 파일들에서 모은 팀 이름이고, 여러 팀이 같은 사람을 적어
냈으면 **한 칸에 쉼표로 잇는다** (`"AI솔루션팀, 로봇응용기술팀"`). 이 칸이
없으면 부서가 나눈 팀은 1단계를 지나는 순간 사라진다 — 그게 아래 ⑤ 의 뿌리였다.

2단계는 그 칸을 어떻게 쓸지만 고른다. **승계**(`mode="inherit"`)면 그대로 옮기고,
**재배치**(`mode="auto"`)면 버리고 점수로 다시 나눈다. 어느 쪽이든 결과는
배정 결과(플랜) 한 곳에만 남는다.

`Board.place()` 는 **`applicant.team`** 으로 자리를 잡는다 — 짝(`pinned`)으로
면접관이 정해져 있어도 `team_slot[(applicant.team, day, hour)]` 에 앉히고
`Assignment.team` 도 거기서 나온다. 그 `applicant.team` 은 04 가 02(배포안)에서
받아 온 값이다.

→ **화면에서만 팀을 바꿔 보여 주면 안 된다.** 부서가 남의 팀 사람과 짝을 지으면
같은 팀 동시간 중복 금지(규칙2)가 엉뚱한 팀에 걸리고, 시간표의 팀 열도 틀어진다.
팀을 바꾸려면 `POST /api/v1/distribute/{plan_id}/adjust` 로 **플랜을 고쳐야** 한다
(`moves: [{applicant_id, from, to, reason}]`, 옮긴 사람에 `HR_MANUAL` 태그).
`adjust` 는 **`from` 으로 지목한 자리 하나만** 옮긴다. 같이 보는 사람은 행이 둘이라
한쪽 팀만 바꾸는 조정이 되고, 나머지 자리는 그대로 남는다. 이미 그 사람이 있는
팀으로 옮기려 하면 409 다(한 팀이 같은 사람을 두 번 보게 된다). 주 팀 자리를
옮기면 같이 보는 자리의 `primary_team` 도 따라간다. **없던 중복을 새로 만들 수는
없다** — 중복은 `담당팀` 에 팀이 둘 이상 적혀 있거나(승계), 재배치에서
`duplicate_score_threshold` 를 넘을 때(자동) 생긴다.

### `rule_compliance()` 는 인자를 다 쓰지 않는다
`rule_compliance(assignments, interviewers, applicants, ...)` 에서 실제로 보는
것은 `assignments` 뿐이다 (확인함). 나머지 두 인자를 바꿔도 결과가 안 변한다.

### 면접관 가용성은 04 의 회차 API 로 판단하면 안 된다
`/api/v1/interviewers/rounds/{round}` 의 `availability` 가 비어 있어도 실제로는
시간이 있는 사람이 많다 — 스케줄러가 배치할 때 03(response-collector)에서
다시 가져오기 때문이다. **콘솔에서 "시간 미제출" 을 짐작하지 마라.** 한 번
그렇게 만들었다가 전부 되돌렸다. 판단은 만들어진 시간표 결과로만 한다.

### Windows · PowerShell 함정
- `.ps1` 에 한글이 있으면 **UTF-8 BOM 필수**. BOM 없으면 PS 5.1 이 cp949 로 읽어
  엉뚱한 파싱 오류를 낸다.
- `git commit -m @'...'@` 는 메시지에 큰따옴표가 있으면 깨진다 → `git commit -F <파일>`.
- 포트 점검은 `Get-NetTCPConnection` 의 `OwningProcess` 로는 부정확하다
  (소켓을 만든 프로세스가 찍힌다). 실제 TCP connect 로 찔러 봐야 한다.
- 여러 스크립트에 같은 경로를 일괄로 넘기다 콘솔 파일을 날린 적이 있다.
  **스크립트마다 argv 계약을 확인하고 넘길 것.**

### 저장소에 넣지 않는 것
지원자 개인정보와 대용량 파일 — `docs/`, `data/`, `**/*.xlsx`, `**/*.sqlite`,
`**/storage/`, 시연 영상(134MB). git 과 Docker 이미지 양쪽에서 뺀다.

---

## 4. 지금까지 한 일 (왜 했는지 중심으로)

### ① 감시 루프 — `fcf82eb`
포트의 서비스가 죽으면 그 서비스만 되살린다. 로컬은 `watchdog.ps1`,
Railway 는 컨테이너 기동 스크립트 안에서 같은 일을 한다. 로컬에서 코드를
고쳐도 반영이 안 되면 **서비스가 `--reload` 없이 떠 있는지** 부터 본다.

### ② 시간표를 부서의 짝짓기에 종속시킴 — `7000e52`
증상: 4단계에서 ② 는 "면접 못 보는 지원자 67/83", ③ 은 "시간표에 들어간 사람
83/83" 이라고 서로 다른 말을 했다.

원인: 콘솔이 시간표를 만들 때 회차·계획 번호만 보내고 **부서가 정한 짝은 보내지
않았다.** 04 에는 짝이라는 개념 자체가 없어서, 부서가 보낸 19쌍 중 17쌍을
시간표가 조용히 다른 담당자로 바꿔 놓고 있었다.

고친 방향 — *시간표가 정하는 것은 시간뿐이고, 누가 볼지는 부서가 정한다*:
- `GenerateRequest.pairs` → `GenerateConstraints.pairs` → `Board.pinned`
- 짝이 있는 지원자는 후보를 고르지 않는다. 그 담당자가 그 시간에 안 되면 다른
  사람을 세우지 않고 **그 시간을 포기한다.**
- `schedule_service.generate()` 는 짝 있는 사람만 배치 대상으로 삼고 나머지는
  미배정으로 넘긴다. 모수(`total_applicants`)는 회차 전체 인원 그대로 둬서
  ② 와 ③ 을 더하면 전체가 되게 했다.
- `pairs=None` 이면 예전처럼 자유 배정(기존 호출자·테스트 보호), `{}` 는
  "부서가 아직 아무도 안 골랐다" 는 뜻이라 시간표를 만들지 않고 이유를 돌려준다.

실측(R20260731-01): 19명 전원 배치, 짝이 바뀐 배정 0건, 하드 위반 0건,
19 + 64 = 83 으로 화면 숫자가 맞았다. Railway 배포까지 확인 완료.

### ③ 순서가 깨지던 문제 · 진행 조건이 반영 안 되던 문제

**증상 1** — 부서 화면 ③ 우리 팀 면접 시간표에서 석사가 흩어져 있었다.
2단계에서 잡는 "날짜마다 학력 고르게 · 하루 안에서는 박사→석사→학사" 가 안 먹은 것처럼 보였다.

**원인** — 알고리즘(`order_for_interview`)은 멀쩡했다. `publish_handoff()` 가
문제였다. 2단계 차례표(`doc["order"][팀]`)는 **그때의 팀 나눔** 으로 만들어졌는데
3단계에서 보내는 명단은 **다시 나눈 결과** 라, 전극기술팀 15명 중 6명만 차례표에
있었다. 나머지 9명은 `slot().get("order") is None` 가지를 타고 **도착 순서로 뒤에
줄줄이 붙었고**, 그 자리에서 학력 묶음이 깨졌다. 게다가
`order_planned = bool(seq)` 라 6명만 맞아도 True 여서, 부서 화면은
"인사 담당자가 잡아 보낸 차례 그대로" 라고 **거짓말** 을 하고 있었다.

**고침** — 차례표가 그 팀 명단을 **남김없이** 덮을 때만 그 차례를 쓴다.
한 사람이라도 빠지면 2단계와 **같은 알고리즘으로 팀 전체를 다시 잡고**
`order_planned=False` 로 정직하게 표시한다.

**증상 2** — 2단계에서 정한 면접 진행 조건(시작 시각·한 사람당 분·쉬는 시간·
하루 인원)이 4단계 만들어진 시간표에는 반영되지 않았다 (09시·10시로만 나왔다).

**원인** — 그 조건은 어디에도 저장되지 않고 2단계 화면 안에서만 살아 있었다.
스케줄러는 고정 교시 계약을 쓴다(3 절).

**고침** — `save_order()` 가 `doc["timing"]` 에 조건을 함께 남기고,
`round_timing()` 으로 어디서나 다시 읽는다. 4단계 시간표와 면접 목록은
`hour_clock()` 으로 칸을 실제 시각으로 갈아 그리고(오후는 점심 뒤부터),
부서 화면 ③ 의 입력칸도 2단계 값이 기본값으로 뜬다. 스케줄러의 계약은
건드리지 않았다.

검증: 실제 회차 데이터로 재생해 1일차 석사 3명 연속, 2일차 석사 2명 연속으로
학력 묶음이 살아나는 것을 확인했다.

### ④ ③ 만들어진 시간표가 지금 상태에 종속되지 않던 문제

**증상** — ② 는 "면접 못 보는 지원자 2/83" 인데 ③ 은 "시간표에 들어간 사람
83/83" 이었다. ②·③ 이 서로 다른 말을 했다.

**원인 (두 가지)**
1. **시나리오 실행 탭**이 `/schedules/generate` 를 **짝 없이** 불렀다. 그러면
   전원 자유 배정(83/83)이 되고, 그 시간표가 같은 회차 목록의 **맨 위**에 올라간다.
   ③ 은 목록의 최신 것을 기본으로 보여 주므로, 부서 짝으로 만든 시간표 대신
   그 시간표를 보고 있었다.
2. ③ 에는 **지금 짝과 견주는 장치가 없었다.** 회차에 남아 있는 아무 시간표나
   '지금 상태' 처럼 보여 줬다.

**고침**
- `schedule_vs_pairs()` — 보고 있는 시간표를 지금 부서 짝과 견준다
  (짝이 바뀐 사람 · 짝도 없이 들어간 사람 · 짝은 있는데 못 들어간 사람).
  어긋나면 ③ 맨 위에 빨간 글씨로 "이 시간표는 지금 부서가 보낸 짝으로 만든 것이
  아닙니다 — 다시 만들어 주세요" 라고 말한다. 시간표 고르는 목록에도
  "⚠ 지금 부서 짝 N명과 다름" 을 붙인다.
- 시나리오 실행도 그 회차에 짝이 있으면 그 짝을 지켜 만든다.
- `handoff_pairs()` · `submit_team()` · `assign_team()` — **그 팀에 보낸 명단 안의
  사람** 과 **그 팀 담당자** 로 맺은 짝만 인정한다. 부서는 받은 목록에서 고르는
  것이지 목록 밖의 사람을 넣을 수 없다. 명단을 다시 보내 팀 구성이 바뀌면 옛
  짝은 여기서 걸러지고, 걸러진 건수는 ② 에 노란 글씨로 알린다 (조용히 사라지면
  숫자만 안 맞는다).

실측(R20260731-01, 짝 19명): 짝으로 만든 `532819bb` 는 배정 19/83 에 어긋남 0,
예전 `e3abcad6` 는 83/83 이지만 **짝이 바뀐 사람 17명 · 짝도 없이 들어간 사람
64명** 으로 잡힌다.

### ⑤ 2단계 팀 나눔이 3단계에서 무너지던 문제

**증상** — 2단계 순서표에서 AI솔루션팀은 가바롬 · 도보나 · 미나래였는데, 부서
화면 '면접자 담당자 매칭' 에 가 보니 벼라희(2단계에서는 전극기술팀)가 들어와
있었다. 실측: 같은 83명인데 **54명이 다른 팀**, 전극기술팀은 20명 → 15명.

**원인** — 2단계는 **어느 명단으로 할지 고르게** 되어 있다 (1단계에서 올린
`희망지원자_{팀}` 파일 vs ① 에서 다시 계산한 배정 결과). 그런데 3단계
`render_send` 는 **언제나 플랜**(`plan_applicants(plan_id)`)으로 보낸다.
파일 쪽을 골라 차례를 잡으면, 인사 담당자가 본 나눔은 아무 데도 남지 않는다.
(이게 ③ 에서 고쳤던 '석사 흩어짐' 의 진짜 뿌리였다 — 그때는 차례만 다시
잡아 증상을 눌렀고, 팀 나눔 자체는 그대로 어긋나 있었다.)

**고침** — 화면에서 맞추지 않고 **플랜을 맞춘다** (위 '팀 나눔의 출처' 참고).
- `split_map()` · `team_moves()` — 2단계 나눔과 플랜을 견줘 옮길 사람을 뽑는다.
  이미 그 팀들 중 하나에 있으면 건드리지 않는다.
- `render_team_gap()` — 어긋나면 빨간 글씨 + 명단 + **[배정 결과를 2단계 나눔에
  맞추기]** 버튼(`/adjust`). 2단계(고른 자리)와 3단계 양쪽에 둔다.
- 3단계 **[명단 보내기] 는 어긋난 동안 눌리지 않는다.** 이게 종속성의 마지막 문.

실측(R20260731-01): 옮길 사람 53명 · 두 팀이 함께 보겠다 한 5명(가나다순 앞
팀으로만 감) · 맞춘 뒤 어긋남 0, 다섯 팀 모두 2단계 차례표가 명단을 남김없이
덮어 `order_planned=True` (즉 2단계에서 잡은 학력 묶음이 부서 화면까지 그대로 산다).

### ⑥ 1단계에서 명단을 지워도 뒤 단계가 살아 있던 문제

**증상** — 1단계에서 [이번 회차 올린 파일 모두 지우기] 나 [맞춰 보기] 를 눌러도
2단계에 가면 지운 명단으로 만든 팀 나눔이 그대로 있었다.

**원인** — 콘솔은 `reset_round_state()` 로 **화면 상태만** 버렸다. 서버의 배포안 ·
회신 · 시간표는 그대로라, 2단계가 `round_plan_id()` 로 옛 배포안을 다시 집어 온다.
게다가 회차 단위로 지우는 API 가 **01 에만** 있었다.

**고침** — 회차를 통째로 비우는 `DELETE` 를 02 · 03 · 04 · 05 에 새로 만들고,
콘솔의 `reset_round_downstream()` 이 넷을 한 번에 부른 뒤 `data/handoff/{회차}.json`
까지 지운다. 부르는 곳은 세 군데 — **[모두 지우기] · [맞춰 보기] · [올리기]**.
(올리기도 넣은 이유: `reset=true` 면 앞선 파일을 지우므로 지우기와 같은 일이다.)

| 서비스 | 엔드포인트 | 지우는 것 |
|---|---|---|
| 02 | `DELETE /api/v1/distribute/rounds/{round}` | 배포안 · 배정 사유 |
| 03 | `DELETE /api/v1/rounds/{round}` | 요청 · 대상자 · 회신 · 독촉 |
| 04 | `DELETE /api/v1/schedules/rounds/{round}` | 시간표 · 배정 · 규칙 점수 · 회차 면접관 |
| 05 | `DELETE /api/v1/repair/rounds/{round}` | 재편성 이벤트 · 안 · 락 · 시간표 사본 |

- **면접관 마스터(`interviewers`)는 건드리지 않는다** — 회차와 무관하다.
- **06(발송 이력) · 07(감사) 도 건드리지 않는다** — 실제로 일어난 일의 기록이다.
- 자식 행을 먼저 지운다. 대량 `.delete(synchronize_session=False)` 는 ORM
  cascade 를 타지 않기 때문이다.
- `reset_round_state(keep=...)` — [맞춰 보기] 는 이미 그려진 파일 선택칸
  (`v_compare_pick`)을 남긴다. 그린 뒤에 지우면 Streamlit 이 놀란다.

확인은 **살아 있는 DB 사본에 대고** 돌렸다 (실데이터 손대지 않음).
R20260731-01 기준 02: 배포안 1 · 배정 123, 03: 요청 1 · 대상자 22,
04: 시간표 2 · 배정 102 · 규칙 10 · 회차 면접관 22, 05: 0.

### ⑦ 담당자 명단을 올리면 화면이 죽던 문제 — `7579e15`

**증상** — 부서 1단계에서 명단을 올리면 `KeyError: '면접 참여'` 로 화면 전체가 죽었다.

**원인** — [팀 골라 보기](`i_team_f`)는 고른 팀을 화면에 기억한다. 새 명단의 팀
이름이 예전과 다르면 남은 팀이 0개 → 보이는 사람 0명 → `pd.DataFrame([])` 에는
열이 하나도 없어 바로 아래 `edited["면접 참여"]` 에서 죽는다.

**고침** — 명단을 등록할 때(엑셀·자동 생성 둘 다) `i_team_f` 를 버리고, 보이는
사람이 0명이면 표를 만들지 않고 안내만 한다. 확인은 `streamlit.testing.v1.AppTest`
로 머리 없이 띄워서 했다 — 고치기 전 예외 1, 고친 뒤 0.

### ⑧ [이대로 확정] 을 다시 누르면 409 가 나던 문제

**증상** — 2단계에서 [이대로 확정] 을 여러 번 누르면
`status 409: VALIDATION_FAILED: status=approved 상태에서는 승인할 수 없습니다`.

**원인** — 서버가 맞다. `approve_plan` 은 상태가 `draft`·`adjusted` 일 때만
받는다(02 `plan_service.py`). 화면 쪽 문제였다 — 확정한 뒤에도 버튼이 그대로
살아 있었고, `fetch_json` 이 캐시(120초)라 다시 그려도 상태가 `approved` 로
안 바뀌어 보였다.

**고침**
- 확정된 배정안이면 [이대로 확정] · [다시 하기] 를 눌리지 않게 하고,
  **누가 언제 확정했는지**와 고치는 방법([사람 옮기기] 로 옮기면 `adjusted` 가
  되어 다시 확정할 수 있다)을 알린다.
- 확정·되돌리기가 성공하면 `st.rerun()` — 바뀐 상태가 바로 보인다.
- [사람 옮기기] 가 성공하면 같은 화면에서 확정 버튼을 바로 살린다(응답의 상태를
  그대로 쓴다).
- 02 의 `GET /api/v1/distribute/{plan_id}` 가 `created_at`·`created_by`·
  `approved_at`·`approved_by`·`reject_reason` 을 빠뜨리고 있었다. 채웠다.
  (그전까지 ③ 의 "…님이 만든 배정입니다" 캡션은 `None · None` 이었다.)
- `say()` 를 대소문자 안 가리게 고쳤다. 서버는 `approved` 로 주는데 표는
  `APPROVED` 로 갖고 있어 상태 칸에 영문이 그대로 나왔다.

### ⑨ 4단계 시간표에 '중복면접' 표가 안 붙던 문제 · 왜 8칸이 아닌가

**증상** — 2단계 순서표에는 중복면접 뱃지가 붙는데 4단계 시간표에는 하나도
안 붙었다. 함께 "하루 8명으로 잡았는데 시간표는 왜 6칸인가", "왜 중간 시간에만
한 명 잡힌 날이 있나" 는 물음이 있었다.

**원인**
- 뱃지: `render_timetable` 이 중복 여부를 **시간표 자체**(`assignments`)에서
  셌다. 배정은 한 사람을 한 팀에만 붙이고 시간표에도 한 줄로만 들어오므로
  중복이 나올 수가 없어 `duplicated` 는 언제나 빈 집합이었다. 중복면접은
  1단계에 올라온 팀별 희망 명단(`희망지원자_{팀}`)에서만 보인다 — 2단계가
  쓰는 출처와 같다.
- 6칸: `shared/contracts/constants.py` 의 `HOURS` 가 여섯 개다. 담당자에게
  가능한 때를 **오전(09·10·11시) · 오후(14·15·16시) 두 덩어리**로만 묻기
  때문이다. 특정 담당자가 바빠서가 아니다 — 실제로 이번 회차는 팀마다 두
  명만 시간을 적어 냈고, 안 적은 사람은 `_merge_availability` 가 전부 가능한
  것으로 본다.
- 중간 시간만 잡힌 날: `algorithm_v5.DAYS_PER_TEAM = 3` — 한 팀을 3일
  (3일 × 6칸 = 18명) 안에 몰아 넣고, 넘친 인원은 3단계 `fallback_place` 가
  다른 날에 끼운다. 이때 **첫 타임(09시·14시)을 새로 여는 데 +12** 벌점이
  있어 10·11·15·16시로 밀린다. (이번 회차에서 한 팀이 하루 1~2명만 있는
  칸 10개는 전부 이 fallback 이었고, fallback 시간대는 15시가 가장 많았다.)

**고침** (`tools/test_console.py`)
- 4단계 뱃지를 2단계와 같은 출처(`team_rosters_from_versions`)에서 세고,
  이번 시간표에 들어온 사람만 표를 단다. 밑의 표에는 함께 보려는 팀과
  이 시간표에서 만나는 팀·시각을 같이 적었다.
  → **⑩ 에서 되돌렸다.** 자리가 둘로 나가게 된 뒤로는 시간표 자체에 두 줄이
  들어오므로 1단계 파일을 다시 읽을 이유가 없다. `team_rosters_from_versions`
  는 지웠다.
- 2단계 `per_day > 6` 경고에 **왜 6칸인지**(오전·오후 덩어리로만 받은 가능
  시간)와 몇 명이 다음 날로 밀리는지를 적었다.
- 4단계에 "빈 칸이 왜 생기나요" 설명을 접이식으로 붙였다(3일 몰아 보기 ·
  첫 타임 회피 · 시간 안 적은 담당자는 모두 가능으로 본다).
- 개별 카드를 'fallback' 이라고 표시하지는 않았다. `HR_MANUAL` 태그는
  스케줄러 3단계뿐 아니라 02 `_ensure_min_tags` · 05 repair 도 붙여서
  그것만으로는 사유를 못 가린다.

→ **여기 적은 '왜 6칸인가' 는 ⑪ 에서 뒤집혔다.** 담당자 여력 때문이 아니라
계약의 `HOURS` 가 여섯이었을 뿐이다. 고쳐야 할 것을 설명으로 덮은 셈이다.

### ⑩ 담당팀을 원본으로 삼고 중복면접을 실제 두 자리로 — `2b1c08e` · `be11c11`

세 가지를 한 흐름으로 고쳤다. 시작은 "2단계에서 나눈 팀과 지금 배정 결과가
53명 다릅니다" 라는 경고였다 — ⑤ 에서 만든 그 경고가 **매번 뜨는 게 정상** 이
되어 있었다. 재배치가 부서의 나눔을 늘 버리기 때문이었다.

**1) 취합파일에 `담당팀` 컬럼** (`01 merge_service.py`)

`team_assignment_map()` 이 활성 팀 배포본에서 `team_name` 을 모으고,
`_write_team_column()` 이 행마다 채운다. 컬럼이 이미 있으면 **덮는다** — 한 번
병합한 취합본을 다시 기준으로 삼는 경우가 있어 붙이기만 하면 같은 이름의 컬럼이
늘어난다. 병합 응답에 `team_column`·`teamed_count`·`team_duplicate_count`·
`teamless` 를 실어 화면이 바로 보여 준다.

원본 열 순서를 지키려고 **맨 뒤에 붙는다**(54번째). 콘솔 미리 보기에서만 맨 앞으로
당겨 그린다 — 안 그러면 가로로 한참 밀어야 보여서 "안 생겼다" 고 오해한다.
(실제로 그렇게 오해했다. 그때 보던 취합본은 이 코드가 생기기 전 파일이었다.
**병합할 때 한 번 새기는 값이라 옛 취합본은 저절로 바뀌지 않는다** — 다시 만들어야 한다.)

**2) 2단계는 두 갈래, 배타적으로** (`02 distributor_engine.inherit`)

- **[팀 배정하기]** = 승계. 정원도 학위비율도 안 건드린다. 조직 조건에 안 맞아도
  팀이 골랐으면 그 팀에 남기고 `ORG_UNMATCHED` 표시만 한다.
- **[명단 재배치]** = 기존 `distribute()`. `같이 볼 기준 점수` 는 이쪽에서만 쓴다.
- 중복을 설계상 받아들이므로 `두 팀이 같이 보기 허용` 체크박스는 없앴다.

`담당팀` 이 **빈 사람만** 점수로 나눠 담는다(`_auto_fill`, `AUTO_FILL` 태그).
남은 정원 위에서 계산하므로 승계로 찬 팀에는 덜 간다. 전에는 그런 사람을
`unassigned` 로 빼 두었고, 전원이 공란이면 400 으로 막아 **전체 명단만 올린
회차는 [팀 배정하기] 가 눌리지도 않았다.** 지금은 그냥 전원 분산이 된다.

**3) 같이 보는 사람은 자리가 둘 — 면접도 두 번**

뿌리는 `02 plan_service.get_plan_applicants` 가 `is_duplicate` 행을 **걸러서**
내려보낸 것이었다. 팀에 나가는 엑셀(`export.py`)에는 그 사람이 있는데 시간표에는
한 팀에만 잡혀서, 나머지 팀은 면접 볼 사람이 시간표에 없었다. 필터를 열자 그동안
가려져 있던 것 둘이 같이 드러났다.

- **`Board` 에 지원자 시간 충돌 규칙이 없었다.** 같은 사람이 같은 시각에 두 팀
  면접장에 앉을 수 있었다 — 팀도 면접관도 다르니 규칙1·2로는 안 걸린다.
  `applicant_slot[(applicant, day, hour)]` 를 더해 구조적으로 막고, 보드를 거치지
  않은 시간표를 위해 `constraint_checker` 에 `APPLICANT_CONFLICT` 를 넣었다.
- **짝이 지원자 사번 하나로만 묶여 있었다.** 두 팀이 각자 담당자를 골라도 나중에
  읽은 팀 것이 앞 팀 것을 덮었다. 짝을 **(팀 × 지원자)** 로 바꿨다 —
  `pairs_by_team` 이 `GenerateRequest` → `GenerateConstraints` → `Board.pinned_by_team`
  으로 흐르고, 콘솔은 `handoff_pairs_by_team()` 으로 팀 구분을 살려 읽는다.
  옛 `pairs` 도 그대로 받는다(팀별 짝이 있으면 그쪽이 이긴다).
  `find_interviewer` 가 **남의 팀 담당자를 그 자리에 세우던 것** 도 막았다.

`_team_counts` 는 일부러 주 팀 자리만 센다 — 그 숫자가 `distribute()` 안 정원
계산을 먹인다. 그래서 2단계 ②와 ③의 인원이 다른데, 조용히 맞추지 않고 캡션으로
설명해 두었다. 사람 수로 세면 틀리는 자리의 문구는 **'자리 · 건'** 으로 바꿨다.

**확인** — 01 47 · 02 122 · 03 176 · 04 146 통과, 콘솔 컴파일.
새 테스트: `02/tests/test_inherit.py`, `04/tests/test_duplicate_seats.py`.
실데이터(R20260731-01, 팀 배포본 5개 · 83명)로 다시 취합하면 `담당팀` 이
**공란 0 · 한 팀 78 · 두 팀 5** 로 채워지는 것을 DB 읽기 전용 조회로 미리 확인했다
(예: `3672536 → AI솔루션팀, 로봇응용기술팀`).

### ⑪ 하루 8칸으로 — 칸 이름을 시각에서 자리 번호로 — `4ab2b8a`

**증상** — "면접 타임을 최대 8슬롯 쓰겠다고 했는데 4단계는 왜 6칸이라고
경고하나". ⑨ 에서 붙인 그 경고가 맞는 말이긴 했지만, 고쳐야 할 것을 설명으로
덮고 있었다.

**원인** — 서로 다른 숫자 셋이 한 이름처럼 쓰이고 있었다.

| 무엇 | 어디 | 뜻 |
|---|---|---|
| `SLOTS_PER_DAY` | 콘솔 | 2단계에서 하루에 몇 명 볼지 |
| `MAX_SLOTS_PER_DAY` | `board.py` | 담당자 **한 사람**의 하루 상한 |
| `len(HOURS)` | 계약 | 시간표의 칸 수 |

규칙2(같은 팀 동시간 중복 금지)가 `(팀, 날, 칸)` 을 하나로 묶으므로, 팀당 하루
상한을 실제로 정하는 것은 `len(HOURS)` 였다. 그게 6이라 8을 쓸 수 없었다.
담당자 여력과는 아무 상관이 없었다.

**고침**

1. **`HOURS` = `1타임`…`8타임`.** 시각으로 이름 붙이면 진행 조건이 바뀌는 순간
   이름이 거짓말을 한다 — 30분·쉼 5분이면 "13시" 칸의 실제 시각은 11시 20분이다.
   자리 번호는 어떤 조건에서도 틀리지 않는다.
2. **점심을 따로 두지 않는다.** 하루는 1타임~8타임 한 덩어리다
   (`rule_evaluator.BLOCKS`, `constraint_recheck.HOUR_BLOCKS` 가 각각 한 덩어리).
   `hour_clock()` 은 이제 `slot_labels()` 를 그대로 부른다 — 3단계 순서표와
   4단계 시간표가 **같은 계산**을 쓰므로 날짜가 어긋날 자리가 없다.
3. **오전·오후는 계산 결과다** (3 절). 기본 조건에서 6타임(11:55~12:25)은
   정오에 걸치므로 '오전만' 인 사람도 '오후만' 인 사람도 못 앉는다 — 이건 버그가
   아니라 사실이다.
4. **못 채우는 자리는 숨기지 않는다.** `team_seat_gap()` 이 팀마다 '아무도 맡을
   수 없는 칸'을 찾아 *왜* 그런지와 함께 알린다("… '오전·오후' 가능한 담당자가
   더 필요합니다"). 3단계 현업부서 매칭 화면과 4단계 양쪽에 뜬다.
5. **`담당자 일정 무시하고 배치하기`** 체크박스. `GenerateConstraints.
   ignore_availability` → `Board.ignore_availability` → `_fits()` 로 흐른다.
   사람을 더 못 구할 때 쓰는 탈출구다.
6. **일부러 고른 어긋남은 위반이 아니다.** 이 체크박스를 켜고 만들면 예전에는
   `AVAILABILITY_VIOLATION` 이 그대로 쌓여 "규칙 위반 12건" 이라고 겁을 줬다 —
   인사가 스스로 고른 결과를 놓고 경고를 받는 셈이다. 이제 `check_hard_
   constraints(…, ignore_availability=True)` 는 그 항목을 빼고, 대신
   `off_band()` 가 **누구와 다시 이야기해야 하는지** 를 명단으로 낸다
   (`/generate` 의 `off_band` · `off_band_count`, `/validate` 의 `off_band`).
   콘솔은 `render_off_band()` 로 팀·담당자·면접자·시각·사유를 표로 보여 준다.
   켰다는 사실은 `rule_compliance` 의 `overall` 행 JSON 에 적어 둔다 —
   `schedules` 표에 칸을 늘리면 쓰던 DB 를 못 읽기 때문이다. 그래서 나중에
   다시 검증해도 같은 잣대를 쓴다.

**덤으로 잡은 것**

- 팀 화면 ② 의 정원 계산이 **하루 상한을 전체 상한으로** 쓰고 있었다. 그래서
  사람이 리더에게 몰리고 없는 인력난을 알렸다. `× SCHED_DAYS_PER_TEAM` 을 곱한다.
- `band_cap(BAND_NONE) or SLOTS_PER_DAY` 가 '어려움' 인 사람에게 8칸을 줬다
  (0 은 falsy 다). `max(0, …)` 로 0 을 0 으로 둔다.
- 06 의 계약 사본이 원본과 벌어져 있었다(`Field` 미import · `Applicant.
  assigned_teams` 누락). 같이 맞췄다.
- `constraint_checker._availability_map` 이 `max_daily` 기본값을 아직 **6** 으로
  두고 있었다(칸이 여섯이던 시절의 잔재). `len(HOURS)` 로 고쳤다.
- 팀별 시간표와 히트맵만 아직 `1타임` 같은 자리 번호를 그대로 내보이고 있었다.
  바로 위 '전체 면접 목록' 은 시각으로 적고 있어서 같은 화면에서 두 말이
  섞였다. 둘 다 `clock` 을 태운다.

**확인** — 01 47 · 02 122 · 03 176 · 04 154 · 05 82 · 06 129 · 07 119 전부 통과,
콘솔 컴파일 · 7개 화면 렌더 예외 0, 7개 서비스 기동 후 `integration-tests`
22개 통과, `tools/test_runner.py scenario happy` 108건 배정 · 하드 위반 0.
새 테스트: `04/tests/test_api_interviewer_roster.py::test_legacy_clock_hours_are_moved_into_current_slots`,
같은 파일의 `test_ignore_availability_fills_seats_and_names_who_to_call`,
`04/tests/test_constraint_checker.py` 의 `off_band` · `ignore_availability` 6건.

이 마지막 통합 테스트가 **배정 0명** 으로 처음 떨어졌고, 그래서 3 절의
`normalize_availability()` 가 생겼다. 단위 테스트만 봤으면 못 봤을 자리다.

### ⑫ 앞타임 · 뒤타임으로 (겹치게) · 부서 화면도 가능 시간을 지키게

**말** — "담당자가 오전/오후로 나뉘니까 점심시간쪽이 아예 비어버리는 경우가
있네. 앞타임(~2시)/뒤타임(12시~)/둘다 로 바꾸자. 그럼 점심시간은 중복이 되면서
다 커버가 될거다. 그리고 부서에서 면접자 담당자 매칭때는 왜 8시간 다 채우다가
인사담당자의 최종 시간표 출력에 담당자 시간에 따른 배정을 했나. 애초에 우리팀
면접 시간표에도 담당자에게 가능한 시간으로 자동매칭과 면접시간표도 나오게
해야하는것 아닌가?"

두 가지였다.

**(가) 갈라 받던 것을 겹치게 받는다**

정오 하나로 오전 · 오후를 가르면 정오에 걸치는 칸을 어느 쪽도 못 맡는다.
담당자 수가 넉넉해도 그 칸만 빈다 — 화면에 '이 칸은 하루 종일 되는 사람 몫'
이라고 설명까지 붙여 뒀지만, 설명이 필요한 것 자체가 잘못이었다.

앞타임을 **14시까지**, 뒤타임을 **12시부터**로 두면 두 창이 두 시간 겹친다.
겹치는 칸은 양쪽 다 맡을 수 있으므로 빈 칸이 생기지 않는다. 자세한 것은
3 절 '스케줄러의 시간 모델' 을 볼 것.

- `NOON_MINUTES` · `hour_band` · `AM_HOURS` · `PM_HOURS` · `BAND_AM` ·
  `BAND_PM` 은 없어졌다. `FRONT_END_MINUTES` · `BACK_START_MINUTES` ·
  `hour_bands`(복수) · `FRONT_HOURS` · `BACK_HOURS` · `BAND_FRONT` ·
  `BAND_BACK` 이 대신이다.
- `band_slots(band, timing, count)` 를 새로 뒀다. 부서 화면은 '하루에 볼 인원'
  을 20까지 직접 바꿀 수 있어서 `len(HOURS)` 에 묶으면 안 된다.
- `slot_spans(timing, count)` 도 칸 수를 받게 했다.
- **마이그레이션 없음** — 덩어리 이름은 저장하지 않는다(3 절 참조).

**(나) 부서 화면이 8칸을 그냥 채우던 것**

②의 자동배치는 하루 한도까지 죽 채웠고, ③ 시간표는 `index % per_day` 로 칸을
매겼다. 담당자가 언제 되는지는 보지 않았다. 그래서 부서가 본 시간표와 인사팀
최종 시간표(여기는 가용성을 지킨다)가 서로 달랐다 — 부서에서 첫 칸이던 사람이
최종본에서는 뒤로 밀린다.

**몇 번째 자리냐가 곧 몇 시냐** 라는 점을 양쪽 다 쓰게 고쳤다.

- `assign_by_band(queue, order, caps, can, per_day, lead)` — 화면 밖으로 뺀
  순수 함수다. 자리 번호를 맡을 수 있는 담당자 중에서만 고른다. 연달아 보도록
  앞사람부터 몰아 주되, **맡을 수 있는 자리가 좁은 사람을 먼저** 쓴다. 안 그러면
  뒤타임만 되는 분이 남고 그 칸을 아무도 못 맡아 결국 팀장에게 몰린다.
- `pair_schedule(..., bands=...)` — 차례대로 채우는 대신 그 사람의 담당자가
  맡을 수 있는 **가장 이른 빈 자리**에 놓는다. 제약 없는 사람은 예전과 똑같은
  자리에 놓이므로 순서가 흐트러지지 않는다.
- '어려움' 이라 맡을 칸이 아예 없는 사람은 뺄 수 없으니 자리는 주되
  `off_band` 로 표시하고 ③ 위에 경고를 띄운다.
- 기존 escape hatch(`담당자 일정 무시하고 배치하기`)는 그대로다. 켜면 ②와 ③이
  **함께** 무시한다 — 한쪽만 무시하면 또 어긋난다.

**확인** — 01 47 · 02 122 · 03 176 · 04 156 · 05 82 · 06 129 · 07 119 전부 통과
(831건), `integration-tests` 22건 통과, 콘솔 컴파일 통과.
서비스 7개를 띄우고 `tools/test_runner.py scenario happy` 도 돌렸다 —
108건 배정 · 하드 위반 0 으로 예전과 같다(⑨ 와 같은 값). 콘솔 7개 화면을
`AppTest` 로 하나씩 그려 봤고 예외 0건.
새 테스트: `04/tests/test_api_interviewer_roster.py::test_front_and_back_cover_every_slot`
(여러 진행 조건에서 `앞타임 ∪ 뒤타임 == HOURS` · 겹치는 칸이 실제로 있음),
같은 파일 `test_set_bands_accepts_legacy_names`.

`test_legacy_clock_hours_are_moved_into_current_slots` 는 목 데이터의 리더가
뒤타임이 되면서 **회신과의 교집합** 에 걸려 떨어졌다. 옮기는 규칙 자체는
`normalize_availability()` 로 직접 확인하고, API 쪽은 '옛 이름이 남지 않는다 ·
그 사람이 통째로 빠지지 않는다' 만 보게 고쳤다. 교집합은 정상 동작이다.

---

### ⑬ 시간표 중간의 빈 칸 · 자리를 부서에서 물려받기 · 왜 그렇게 됐는지 적기

**말** — "각 부서에서 면접관들이 시간배정해서 인사팀에게 보냈는데 인사팀의 최종
시간표 만들기는 그것에 종속되지 않고 새로 만들어서 중간중간 빈 구멍의 시간표들이
있다. 시간표가 종속되지 않은것인가 아니면 각 부서에서 담당자 정할때 본인들 되는
시간을 고려 안한것인가?" → "확실히 고쳐야 하는건 고치고 그 외는 원래 원칙이
있던건 유지해야한다." → "결국 모든 데이터의 흐름은 이전에 종속적이여야한다. 만약
사용자가 그 결과를 납득하지 못한다면 왜 그 데이터나 시간표가 위치되게 되었는지
기재되어야 한다."

먼저 **빈 칸이 왜 생겼는지부터 갈랐다.** 저장된 시간표를 실제로 세어 보니 원인이
셋이었고, 셋 중 하나만 버그였다.

**(가) 옛 규칙으로 저장된 가능 시간 — 이게 진짜 버그였다** ⑫ 에서 앞타임 ·
뒤타임을 겹치게 바꿨지만, 덩어리 이름은 저장하지 않으므로(3 절) **이미 저장된 칸
목록은 옛 규칙 그대로 남는다.** 오전 = 1~5타임, 오후 = 7~8타임으로 굳어 있고
6타임(정오에 걸침)은 어느 쪽에도 없다 — 그 팀 담당자 전원이 6타임을 못 맡는다.
⑫ 는 새로 받는 회신만 고쳤고 이미 있던 자료는 그대로였다.

→ `schedule_service.rederive_bands(db)` 를 두고 04 가 뜰 때 한 번 돌린다.
저장된 칸 목록을 `band_of()` 로 되읽어 지금 규칙으로 다시 펼친다. 이게 안전한
이유는 `interviewers.availability` 를 쓰는 곳이 `import_roster` · `set_bands` ·
`seed_interviewers` 뿐이고 **셋 다 덩어리에서 펼친 값** 이기 때문이다. 03 의
칸별 회신은 여기 저장되지 않고 읽을 때 `_merge_availability` 로 교집합을 낸다 —
넓혀 놔도 회신이 좁히므로 사람 뜻을 넘어서지 않는다. **`max_daily` 는 손대지
않는다** — 사람이 직접 낮춰 둔 숫자일 수 있고, 칸을 넓히는 것과 더 많이 보게
하는 것은 다른 이야기다.

**(나) 규칙4 — 3명 이하인 조는 그날 첫 칸을 피한다** (`place_group` 의
`prefer = 0 if k >= 4 else 1`). **(다) 짝을 정해 보낸 사람에게 다른 담당자를
대신 세우지 않는다** — 그 담당자가 안 되면 그 칸을 그냥 비운다(`find_interviewer`).
둘 다 **일부러 그런 것이라 그대로 뒀다.** 대신 화면에서 왜 비었는지 말해 준다.

**부서 자리를 물려받는다 (Stage 0)**

짝만 넘기고 날 · 시각은 인사팀이 새로 짜던 것을 고쳤다. 부서 ⑤ [인사 담당자에게
보내기] 가 `submitted.seats` 를 함께 남기고, 인사 4단계가 그것을 `seats_by_team`
으로 넘긴다. `place_dept_seats()` 가 Stage 1(팀별 면접일 확정) 다음 · Stage 1b 앞에서
돌며 부서가 말한 **n일차를 그 팀에 잡힌 날 중 n 번째**로 옮겨 읽는다.

못 앉히면 **억지로 밀어 넣지 않는다** — (다) 와 같은 이유다. 대신
`board.seat_miss[지원자] = 까닭` 을 적어 두고, `Board.place()` 가 그 사람을 다른
칸에 앉힐 때 그 까닭을 `reason_tags` 에 붙인다. 사유는 7 개(`DEPT_SEAT` ·
`SEAT_MOVED_TAKEN` · `_BAND` · `_CAP` · `_BUSY` · `_OWNER` · `_DAY`)로
`shared/contracts/constants.py` 의 `TAGS` 에 있다.

> **주의** — 콘솔은 `TAGS` 를 import 하지 않고 `tools/test_console.py` 안에
> `TAG_LABELS` 를 따로 갖고 있다. 사유를 늘리면 **양쪽 다** 고쳐야 화면에 한글로
> 나온다. 06 의 `app/contracts/constants.py` 는 shared 의 **바이트 사본**이라
> 같이 복사해야 한다.

**왜 그렇게 됐는지 화면에 적는다**

- 4단계 ① 뒤 — `render_seat_moves()`. 지킨 자리 수를 말하고, 옮긴 사람이 있으면
  면접자별 까닭을 표로 붙인다. 응답의 `dept_seats` · `dept_seats_kept` ·
  `dept_seats_moved` 가 그 자료다.
- 4단계 ③ 시간표 — `empty_slot_reasons()`. **쓰인 칸 범위 안쪽**의 빈 칸만 세고
  (맨 앞 · 맨 뒤 여백은 빈 칸이 아니다) 팀 명단의 가능 시간 · 하루 한도로 셋 중
  하나를 답한다: 맡을 사람이 없다 / 하루 한도가 찼다 / 규칙 때문에 비웠다.

`pair_schedule` 이 `slot_no`(칸 번호)를 함께 돌려주게 했다 — 시각 문자열은 진행
조건이 바뀌면 달라지지만 칸 번호는 그대로다(3 절 '자리 번호' 와 같은 이유).

**확인** — 01 47 · 02 122 · 03 176 · 04 169 · 05 82 · 06 129 · 07 119 전부 통과
(844건), `integration-tests` 22건 통과. 04 커버리지 93%.
새 테스트: `04/tests/test_dept_seats.py` 8건 (자리를 그대로 지키는지 · 일차를
그 팀 면접일로 옮겨 읽는지 · 못 지킬 때 `SEAT_MOVED_*` 가 맞는지 · 자리를 안 보내면
예전대로 도는지), `04/tests/test_rederive_bands.py` 5건 (옛 오전 자료를 넓히는지 ·
`max_daily` 를 안 건드리는지 · 두 번 돌려도 그대로인지 · '어려움' 을 안 채우는지).

---

### ⑭ 부서 시간표가 인사팀 최종본에서 절반이 흐트러지던 문제 · 겹친 사람만 다시 앉히기 · 단계 안내

**불만이 무엇이었나**

> "1차 시간표를 인사담당자가 만들어서 넘겼는데 부서에서 자기들 가능한 시간대를
> 매칭하면 거기서 담당자 시간에 맞는 자동매칭이 되었어야 하고 그게 또 종속적으로
> 넘어가서 면접시간표 만들기에 영향을 줘야하고 면접시간표 만들기에서는 최소한의
> 수정만 있었어야 하는데 지금 결과물들이 매번 다 달랐기 때문이다."

인사 → 부서 → 인사를 화면 버튼으로 실제로 눌러 보는 몰이 스크립트를 만들어
재 보니, 부서가 확정해 보낸 자리 64건 중 최종 시간표가 지킨 것이 **31건(48 %)**
뿐이고 **28명은 아예 시간표에 못 들어갔다**.

**(가) 진짜 원인 — 목 데이터가 마스터 가용성을 지우고 있었다**

`load_interviewers()` 는 회차 담당자마다 마스터 가용성과 03 회신을 **교집합**으로
합친다. 그런데 03 에 회신이 0건이면 `AvailabilitySource.fetch()` 가
`USE_MOCK=true` 폴백으로 **목 면접관**을 돌려주고, 그것이 교집합에 들어갔다.
목 IV101 은 7 · 8타임만 되는 사람이라 마스터에 `3~7타임` 이라고 적어 낸 분이
스케줄러 안에서는 `7타임` **한 칸**이 되었다. 어떤 분은 팀 면접일 3일 통틀어
되는 칸이 0개였다 — 부서 화면이 보여 준 시간표와 스케줄러가 믿는 시간표가 서로
다른 물건이었던 것이다.

고친 곳은 한 줄이다. 교집합을 잡는 자리에서는
`availability_source.fetch(round_id, allow_mock=False)` 로 부른다. 회신이 없으면
빈 명단을 받아 마스터 가용성을 그대로 쓴다. 목 폴백이 남는 곳은 **겹쳐 볼
마스터가 아예 없는** 경로(면접관 DB 가 비었을 때)뿐이다.

> 이 버그는 `_merge_availability()` 가 **한 칸도 안 겹칠 때만** 마스터로
> 되돌리는 규칙에 오래 가려져 있었다. 안 겹치면 마스터가 살아남으니 티가 안
> 나고, **한 칸이라도 걸치면** 그 한 칸만 남아 조용히 망가진다. 그래서 회귀
> 시험(`test_master_hours_survive_when_nobody_answered`)은 일부러 한 칸 겹치게
> 잡는다 — 안 겹치게 잡으면 버그가 있어도 통과한다.

**(나) 부서 자동배치가 팀장 한 명을 무너뜨리던 것**

`assign_by_availability()` 는 그 날 그 칸을 맡을 사람이 아무도 없으면 그 자리를
**팀장에게** 얹었다. 하루 한도를 안 보고 얹으니 팀장 혼자 15명(한도로는 12명)을
떠안고, 그 초과분은 인사팀 시간표에서 도로 옮겨졌다. 지금은 **그날 여유가 가장
많은 분**에게 얹는다(같은 여유면 그날 나오시는 분 먼저, 그래도 같으면 앞 차례).
어차피 못 채우는 자리라면 한 사람을 무너뜨리지 말고 나눠 지는 편이 뒤가 덜
흔들린다. 그 칸이 어디였는지는 전과 같이 화면에 적어 준다.

**결과 (같은 재료 · 같은 스크립트)**

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 부서 자리를 그대로 지킴 | 31/64 (48 %) | **60/64 (94 %)** |
| 시간표에 못 들어간 사람 | 28명 | **0명** |
| 자리가 아예 없는 짝 | 3명 | **0명** |
| 두 번 만들어 같은가 | 예 | 예 |
| 최종 시간표의 가능 시간 밖 자리 · 하루 한도 초과 | — | **0건 · 0건** |

남은 4건은 담당자 가능 시간이 진짜로 모자란 칸(8타임)이라, 부서 화면이 안내하고
스케줄러가 까닭을 적어 옮긴다 — 이게 설계대로다.

**(다) 중복면접자가 같은 시각에 걸렸을 때**

부서가 사람을 덜어 내면 그 자리가 다른 사람으로 메워지면서, 두 팀이 같이 보는
사람의 두 자리가 같은 날 같은 칸으로 맞물릴 수 있다. 4단계 [이상 없는지 확인]
이 그걸 짚고, [오류 수정하기] 가 **겹친 그 사람만** 다시 앉힌다
(`duplicate_fix.py`, `POST /api/v1/schedules/{id}/fix-duplicates`). 옮길 때도
담당자 가능 시간과 하루 한도를 지키고, 옮길 데가 없으면 `stuck` 으로 돌려준다.

> 이 엔드포인트를 붙인 뒤 화면에서 눌러도 안 되기에 한참 헤맸는데, 원인은
> **8004 가 예전 코드로 떠 있어서** 였다(`--reload` 없이 띄운 창). 04 를 고쳤으면
> 서비스를 다시 띄우고 확인할 것.

**(라) 처음 쓰는 사람을 위한 안내**

왼쪽 '진행순서' 옆 `?` 에 마우스를 올리면 단계 전체를 설명하고, 6개 화면마다
그 화면에서 눌러야 할 버튼을 ① ② ③ ④ 로 띄운다. 끝난 칸은 지금 상태를 읽어
스스로 접힌다(예: "4개 팀에 명단이 갔습니다", "지금 3/4개 팀 회신").

**확인** — 01 47 · 02 122 · 03 176 · **04 198** · 05 82 · 06 129 · 07 119 전부
통과(873건), `integration-tests` 22건 통과, 04 커버리지 93 %. 화면 7개 예외 0.
몰이 스크립트로 인사 → 부서 → 인사 전 구간을 눌러 위 표를 재확인했다.

---

### ⑮ 담당자 가능 요일을 없애고, 날 이름을 '1일차' 로 — `9109730` · `9cc4d85`

**말** — (가) "가능시간은 알겠는데 가능 요일은 적용 안 해놨는데 옵션에 왜 있나.
모든 과정에서 담당자의 가능한 요일은 아예 빼라. 우리는 어느 요일이건 앞타임 ·
뒤타임 · 모든타임으로만 연산한다." 이어서 (나) "지금 상태에서 미래혁신팀은 왜
첫날 면접이 없나. AI솔루션팀도 그렇다. 월화수목금 그런 거 필요 없이 1일차
2일차 이런 식으로 하면 된다 — 요일은 지금 우리가 고려할 필요가 없는 값이다."

**(가) 담당자 가능 요일을 뺀다** — `9109730`

폼은 처음부터 시간 덩어리만 물었다. 그런데 저장 형식이 `{날: [칸]}` 이라
아무도 고른 적 없는 날이 자료에 딸려 들어갔고, 화면과 배치가 그걸 사람의 뜻인
양 읽었다. "모든 시간이 된다고 적어 냈는데 왜 빈 자리가 없나" 가 거기서 나왔다.
`normalize_availability()` 를 읽는 길목마다 두어 **날을 지우고 칸만 모든 날에
똑같이 편다.** `둘 다` 는 "날도 둘 다인가" 로 읽혀 `모든타임` 으로 바꿨다.

**(나) 왜 첫날 면접이 없었나**

`plan_team_days()` 가 **큰 팀부터 한산한 날을 골라 가게** 되어 있었다. 실제
회차(`R2026-E2E-74903`, 로봇 24 · 배터리 16 · 전극 16 · AI 8 · 미래 8)를 다시
돌려 보면 로봇이 1~3일차를 가져가고, 뒤에 남은 AI솔루션팀은 3~5일차만 받았다.
자료가 잘못된 게 아니라 **날을 나눠 주는 규칙이 그렇게 시킨 것**이었다.

날을 비켜 준다고 얻는 것이 없다. 규칙2(같은 팀 동시간 중복 금지)는 `(팀, 날, 칸)`
단위라 팀끼리는 애초에 안 부딪히고, 담당자는 자기 팀 면접만 본다. 그래서 지금은
**어느 팀이나 1일차부터** `days_per_team` 일을 똑같이 쓴다.

> 인원을 보고 날 수를 늘려 주는 안도 만들어 봤다가 **버렸다.** 그러면 v4 가
> v5 처럼 굴어 두 알고리즘의 커버리지 맞바꿈(v4 규칙 100 % · 커버리지 낮음 /
> v5 그 반대)이 사라진다. 넘치는 인원은 날을 늘려 감추지 말고 '못 앉은 사람'
> 으로 내놓는다 — v5 는 Stage 3 가 흡수한다.

모든 팀이 같은 날부터 보게 되자 **두 팀이 같이 보는 사람**의 두 자리가 같은
시각에 맞물리는 일이 잦아졌다. `place_group()` 이 그 사람만 같은 날 가장 가까운
빈 칸으로 민다(그룹 전체를 흔들지 않는다). 안 고쳤으면 두 번째 자리가 조용히
사라진다 — `test_both_seats_are_scheduled_at_different_times[v4]` 가 그걸 잡는다.

**(다) 날 이름을 1일차 … 5일차 로**

`DAYS` 를 갈았다. 이름이 '월 · 화' 이면 사람이 달력을 떠올려 "우리 팀은 왜
월요일이 없나" 를 묻게 되는데, 우리 셈에는 월요일과 목요일의 차이가 없다.
실제 달력에 언제 잡을지는 인사팀이 따로 정한다.

**(라) 요일은 옮겨 읽지도 않는다** — `2f6f4ef`

처음에는 옛 자료를 물리지 않으려고 `day_name()` · `LEGACY_DAYS` 를 만들어 읽는
길목마다 `"월"` 을 일차로 옮겨 읽었다. 그런데 **7개 DB 를 다 뒤져 보니 요일로
저장된 행이 한 줄도 없었다** — 회차를 다시 돌리면서 전부 일차로 바뀌어 있었다.
쓰는 데가 없는 옮김판은 "어딘가 요일 자료가 있다" 는 거짓 신호만 남긴다. 그래서
통째로 걷어냈다. 요일이 우리 셈에 들어오지 않는다는 말은, 옮겨서라도 받아 준다는
말이 아니라 **아예 다루지 않는다** 는 말이다.

읽는 쪽은 원래부터 달력에 없는 이름을 버리고 있어서(`if d in DAYS`) 손댈 게
없었다. 03 폼 검증만 태도가 바뀐다 — `"월"` 이 오면 조용히 고쳐 받지 않고
`알 수 없는 날: 월` 로 되돌려 준다. 그 회신은 이름을 바꾸기 전에 나간 폼 링크로
낸 것이라는 뜻이라, 받아 두면 어디서 온 값인지 아무도 모르게 된다.
04 스키마에서 날 이름 검증은 그대로 뺀 채다 — 읽을 때 날은 어차피 지워지므로
물릴 까닭이 없고, 물리면 옛 회차가 통째로 안 열린다.

**확인** — 01 47 · 02 122 · 03 177 · **04 204** · 05 82 · 06 129 · 07 119 ·
콘솔 9 = **889건** 전부 통과, `integration-tests` 22건 통과. 화면 7개 예외 0.
`R2026-E2E-74903` 을 다시 돌리니 다섯 팀 모두 1일차부터(6·6·6칸) 잡히고 하드
위반 0건.

새로 넣은 회귀 시험 4건은 **버그를 되돌려 놓고 실패하는지 확인했다** —
① 옛 날 배분을 되살리면 `test_every_team_starts_on_day_one` 실패,
② `fixed_team_days` 의 `if d in DAYS` 를 빼면 `test_a_day_name_off_the_calendar_is_dropped` 실패,
③ `place_group` 의 되밀기를 빼면 `test_both_seats_are_scheduled_at_different_times[v4]` 실패,
④ 03 `validator` 에 `day_name()` 을 되살리면
   `test_a_weekday_is_rejected_and_named_in_the_reason` 실패.

---

## 5. 다음에 할 만한 것

- 이번 순서 수정은 **다음번 3단계 '명단 보내기' 부터** 적용된다. 이미 보낸 회차의
  `data/handoff/*.json` 은 그대로이므로, 확인하려면 3단계를 다시 보내야 한다.
  ⑬ 의 `submitted.seats` 도 마찬가지로 **부서가 ⑤ 를 다시 눌러야** 생긴다.
  옛 회차는 자리가 없어 예전처럼 시각을 새로 짠다 — 빈 칸이 보여도 버그가 아니다.
- `per_day > 8` 은 여전히 막혀 있다. ⑪ 에서 8칸까지 열었지만 `HOURS` 는 아직
  고정 길이라, 회차마다 칸 수를 바꾸려면 스키마 검증(`Hour` 리터럴) ·
  `Interviewer.max_daily` 기본값 · 저장된 가용성 이관을 함께 손봐야 한다.
- 콘솔(`tools/test_console.py`)에는 자동 테스트가 없다. 순서·시각 계산
  (`order_for_interview` · `slot_labels` · `hour_clock` · `publish_handoff`)은
  순수 함수라 떼어내 테스트하기 좋다. `handoff_pairs_by_team` ·
  `schedule_vs_pairs` 도 이제 (팀 × 지원자) 로 키가 바뀌어 같이 두면 좋다.
  ⑫ 에서 뺀 `assign_by_band` · `pair_schedule(bands=…)` 도 순수 함수다 —
  지금은 손으로만 확인했다(자리마다 맡을 수 있는 사람만 들어가는지 · 자리가
  겹치지 않는지). ⑬ 의 `handoff_seats_by_team` · `empty_slot_reasons` 도 같다.
  여기부터 테스트를 붙이면 값이 크다.
- **⑩·⑪·⑫·⑬ 은 `main` 에 올라갔다** (`2b1c08e` · `be11c11` · `4ab2b8a` ·
  `3253027` · `25ed36f` · `f86cf48`, 2026-08-01 push). ⑭ 는 그 뒤 커밋이다.
  `fix/e2e-contract-and-audit-events` 는 오래된
  브랜치로 남아 있다 — 이어서 할 때 헷갈리지 않게 확인하고 정리할 것.
- **04 를 고쳤으면 서비스를 다시 띄우고 확인한다.** `start_all.ps1` 은
  `--reload` 로 띄우지만, 창을 따로 열어 두었거나 헤드리스로 돌린 경우에는
  옛 코드가 그대로 서비스한다. ⑭ 에서 새 엔드포인트가 404 로 나와 한참 헤맸다.
- 몰이 스크립트(인사 → 부서 → 인사 전 구간을 `AppTest` 로 눌러 보고 지킨 자리
  비율 · 재현성 · 겹침 수정까지 재는 것)는 저장소에 없다. ⑭ 처럼 "종속이
  끊겼다" 는 말이 나오면 다시 만들어 숫자로 재는 편이 빠르다. 화면 키는
  인사 `c_publish` · `s_generate` · `s_validate` · `s_fixdup` · `s_lock`,
  부서 `tv_all` · `tv_auto_go` · `tv_submit` · `tv_send_hr` 이고, 역할 이름은
  `"인사 담당자"` · `"현업 부서 (면접관)"` · `"시스템 관리자"` 로 **정확히**
  넣어야 한다(줄여 쓰면 조용히 인사 화면이 그려진다).
- 옛 취합본에는 `담당팀` 이 없다. 회차를 이어서 볼 때는 **확정 명단부터 다시**
  만들어야 [팀 배정하기] 가 제 뜻대로 돈다.

## 6. 이어서 할 때 첫 5분

```powershell
.\start_all.ps1          # 서비스 7개 + 콘솔 (코드 고칠 거면 --reload 로 떠 있는지 확인)
.\test_all.ps1           # 전체 테스트 (2026-08-01 기준 01 47 · 02 122 · 03 177 ·
                         #  04 204 · 05 82 · 06 129 · 07 119 · 콘솔 9 = 889건)
```

콘솔은 http://localhost:8501 . 스케줄러 테스트만 돌리려면
`cd services/04-scheduler; pytest -q`.
