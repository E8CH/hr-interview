# 작업 이력 · 이어서 하기 (MEMORY)

다른 PC 에서 `git clone` · `git pull` 로 이 저장소를 받아 바이브코딩을 이어갈 때
**가장 먼저 읽는 문서**다. 코드나 커밋 로그만 봐서는 알 수 없는 것 — 왜 그렇게
만들었는지, 어디에 함정이 있는지, 지금 어디까지 왔는지 — 만 적는다.
(코드 구조 설명은 `docs/`·`bmad/`·각 서비스 README 에 있다.)

최종 갱신: 2026-07-31

---

## 1. 이 시스템이 무엇인가

LG 해커톤 과제 — HR 면접 일정 자동 생성. FastAPI 마이크로서비스 7개와
Streamlit 콘솔 1개로 되어 있다.

| 포트 | 서비스 |
|------|--------|
| 8001 | version-manager |
| 8002 | distributor |
| 8003 | response-collector |
| 8004 | scheduler |
| 8005 | repair-engine |
| 8006 | notification-hub |
| 8007 | audit-analytics |
| 8501 | 콘솔 (`tools/test_console.py`) |

로컬은 Windows/PowerShell (`start_all.ps1`), 배포는 Railway 의 리눅스 컨테이너
하나에 전부 띄우고 **8080 포트로 콘솔만** 노출한다.

## 2. 업무 흐름 (이게 제품의 뼈대다)

```
인사 ① 지원자 명단 올리기
인사 ② 팀별 명단 나누기 + 면접 차례 잡기 (면접 진행 조건도 여기서 정한다)
부서 ① 우리 팀 면접 담당자 정하기
인사 ③ 각 팀에 명단 보내기            → data/handoff/<round>.json
부서 ② 면접자–담당자 매칭 후 인사에 제출
인사 ④ 면접 시간표 만들기              → 04-scheduler
```

**뒷 단계는 앞 단계에 종속된다.** 특히 ④ 시간표는 부서가 ② 에서 짝지어 보낸
사람만 대상으로 만든다. 이 종속을 끊으면 화면 안에서 숫자가 서로 어긋난다
(실제로 그랬다 — 4 절 참고).

## 3. 코드만 봐서는 모르는 것들

### 콘솔 ↔ 스케줄러 연결은 서비스가 아니라 파일이다
`data/handoff/<round_id>.json` 한 장이 인사 화면과 부서 화면의 유일한 접점이다.
서비스 API 가 아니다. 구조:

```jsonc
{
  "round_id": "...", "plan_id": "...", "sent_at": "...", "sent_by": "...",
  "order":   { "<팀>": [{applicant_id, name, order, day, time}, ...] },  // 2단계 차례
  "order_at": "...",
  "timing":  { "start": "09:00", "minutes": 30, "rest": 5, "per_day": 8 }, // 2단계 진행 조건
  "teams": {
    "<팀>": {
      "applicants":    [{applicant_id, name, degree_type, major_final,
                         order, order_day, order_time}, ...],
      "order_planned": true,          // 2단계 차례를 그대로 쓴 경우에만 true
      "interviewers":  [...],
      "draft":     { pairs: {...} },  // 부서가 배정만 한 상태
      "submitted": { pairs: {...} }   // 인사에게 제출한 확정 짝 ← 시간표의 입력
    }
  }
}
```

`handoff_pairs()` 는 **`submitted.pairs` 만** 모은다. `draft` 는 시간표에 가지 않는다.

### 스케줄러의 시간 모델은 '교시' 다, 시각이 아니다
`shared/contracts/constants.py` 의 고정 계약이다.

```python
DAYS  = ["월", "화", "수", "목", "금"]
HOURS = ["09시", "10시", "11시", "14시", "15시", "16시"]   # 하루 6칸
AM_HOURS / PM_HOURS                                        # 면접관 가용성이 이 덩어리로 들어온다
MAX_SLOTS_PER_DAY = 8                                      # board.py
```

이 값은 스키마 검증 · 가용성 · rule_evaluator 까지 전부 물려 있어 **함부로 못
바꾼다.** 그래서 "09시" 는 시각이 아니라 **그날의 첫째 칸** 으로 읽어야 한다.
실제 몇 시 몇 분인지는 2단계에서 정한 `timing` 이 정하고, 콘솔이 그려 줄 때
`hour_clock()` 으로 갈아 끼운다.

`hour_clock()` 은 오전 세 칸과 오후 세 칸을 **이어 붙이지 않는다.** 담당자가
'오후만' 을 고르면 스케줄러는 오후 칸에만 넣는데, 시각을 죽 이어 버리면 그 사람이
오전 10시 45분에 앉아 있는 시간표가 나온다. 오후는 점심(최소 1시간, 13:00 이후)
뒤에 다시 시작한다.

→ 한 팀이 하루에 넣을 수 있는 인원은 최대 6명. 2단계에서 `per_day` 를 7 이상으로
잡으면 순서표와 4단계 시간표의 날짜가 어긋나므로 화면이 경고한다.

### 하드 제약은 `Board` 한 곳에만 있다
`services/04-scheduler/app/services/board.py`. v1·v4·v5 알고리즘은 전부
`Board.place()` 로만 배치하므로, **제약이나 고정 짝을 손볼 때 알고리즘은 건드릴
필요가 없다.** v5 = v4 에 `days_per_team=3, fallback=True`.

### `rule_compliance()` 는 인자를 다 쓰지 않는다
`rule_compliance(assignments, interviewers, applicants, ...)` 에서 실제로 보는
것은 `assignments` 뿐이다 (확인함). 나머지 두 인자를 바꿔도 결과가 안 변한다.

### 면접관 가용성은 04 의 회차 API 로 판단하면 안 된다
`/api/v1/interviewers/rounds/{round}` 의 `availability` 가 비어 있어도 실제로는
시간이 있는 사람이 많다 — 스케줄러가 배치할 때 03(response-collector)에서
다시 가져오기 때문이다. **콘솔에서 "시간 미제출" 을 짐작하지 마라.** 한 번
그렇게 만들었다가 전부 되돌렸다. 판단은 만들어진 시간표 결과로만 한다.

### Windows · PowerShell 함정
- `.ps1` 에 한글이 있으면 **UTF-8 BOM 필수**. BOM 없으면 PS 5.1 이 cp949 로 읽어
  엉뚱한 파싱 오류를 낸다.
- `git commit -m @'...'@` 는 메시지에 큰따옴표가 있으면 깨진다 → `git commit -F <파일>`.
- 포트 점검은 `Get-NetTCPConnection` 의 `OwningProcess` 로는 부정확하다
  (소켓을 만든 프로세스가 찍힌다). 실제 TCP connect 로 찔러 봐야 한다.
- 여러 스크립트에 같은 경로를 일괄로 넘기다 콘솔 파일을 날린 적이 있다.
  **스크립트마다 argv 계약을 확인하고 넘길 것.**

### 저장소에 넣지 않는 것
지원자 개인정보와 대용량 파일 — `docs/`, `data/`, `**/*.xlsx`, `**/*.sqlite`,
`**/storage/`, 시연 영상(134MB). git 과 Docker 이미지 양쪽에서 뺀다.

---

## 4. 지금까지 한 일 (왜 했는지 중심으로)

### ① 감시 루프 — `fcf82eb`
포트의 서비스가 죽으면 그 서비스만 되살린다. 로컬은 `watchdog.ps1`,
Railway 는 컨테이너 기동 스크립트 안에서 같은 일을 한다. 로컬에서 코드를
고쳐도 반영이 안 되면 **서비스가 `--reload` 없이 떠 있는지** 부터 본다.

### ② 시간표를 부서의 짝짓기에 종속시킴 — `7000e52`
증상: 4단계에서 ② 는 "면접 못 보는 지원자 67/83", ③ 은 "시간표에 들어간 사람
83/83" 이라고 서로 다른 말을 했다.

원인: 콘솔이 시간표를 만들 때 회차·계획 번호만 보내고 **부서가 정한 짝은 보내지
않았다.** 04 에는 짝이라는 개념 자체가 없어서, 부서가 보낸 19쌍 중 17쌍을
시간표가 조용히 다른 담당자로 바꿔 놓고 있었다.

고친 방향 — *시간표가 정하는 것은 시간뿐이고, 누가 볼지는 부서가 정한다*:
- `GenerateRequest.pairs` → `GenerateConstraints.pairs` → `Board.pinned`
- 짝이 있는 지원자는 후보를 고르지 않는다. 그 담당자가 그 시간에 안 되면 다른
  사람을 세우지 않고 **그 시간을 포기한다.**
- `schedule_service.generate()` 는 짝 있는 사람만 배치 대상으로 삼고 나머지는
  미배정으로 넘긴다. 모수(`total_applicants`)는 회차 전체 인원 그대로 둬서
  ② 와 ③ 을 더하면 전체가 되게 했다.
- `pairs=None` 이면 예전처럼 자유 배정(기존 호출자·테스트 보호), `{}` 는
  "부서가 아직 아무도 안 골랐다" 는 뜻이라 시간표를 만들지 않고 이유를 돌려준다.

실측(R20260731-01): 19명 전원 배치, 짝이 바뀐 배정 0건, 하드 위반 0건,
19 + 64 = 83 으로 화면 숫자가 맞았다. Railway 배포까지 확인 완료.

### ③ 순서가 깨지던 문제 · 진행 조건이 반영 안 되던 문제 (이번 작업)

**증상 1** — 부서 화면 ③ 우리 팀 면접 시간표에서 석사가 흩어져 있었다.
2단계에서 잡는 "날짜마다 학력 고르게 · 하루 안에서는 박사→석사→학사" 가 안 먹은 것처럼 보였다.

**원인** — 알고리즘(`order_for_interview`)은 멀쩡했다. `publish_handoff()` 가
문제였다. 2단계 차례표(`doc["order"][팀]`)는 **그때의 팀 나눔** 으로 만들어졌는데
3단계에서 보내는 명단은 **다시 나눈 결과** 라, 전극기술팀 15명 중 6명만 차례표에
있었다. 나머지 9명은 `slot().get("order") is None` 가지를 타고 **도착 순서로 뒤에
줄줄이 붙었고**, 그 자리에서 학력 묶음이 깨졌다. 게다가
`order_planned = bool(seq)` 라 6명만 맞아도 True 여서, 부서 화면은
"인사 담당자가 잡아 보낸 차례 그대로" 라고 **거짓말** 을 하고 있었다.

**고침** — 차례표가 그 팀 명단을 **남김없이** 덮을 때만 그 차례를 쓴다.
한 사람이라도 빠지면 2단계와 **같은 알고리즘으로 팀 전체를 다시 잡고**
`order_planned=False` 로 정직하게 표시한다.

**증상 2** — 2단계에서 정한 면접 진행 조건(시작 시각·한 사람당 분·쉬는 시간·
하루 인원)이 4단계 만들어진 시간표에는 반영되지 않았다 (09시·10시로만 나왔다).

**원인** — 그 조건은 어디에도 저장되지 않고 2단계 화면 안에서만 살아 있었다.
스케줄러는 고정 교시 계약을 쓴다(3 절).

**고침** — `save_order()` 가 `doc["timing"]` 에 조건을 함께 남기고,
`round_timing()` 으로 어디서나 다시 읽는다. 4단계 시간표와 면접 목록은
`hour_clock()` 으로 칸을 실제 시각으로 갈아 그리고(오후는 점심 뒤부터),
부서 화면 ③ 의 입력칸도 2단계 값이 기본값으로 뜬다. 스케줄러의 계약은
건드리지 않았다.

검증: 실제 회차 데이터로 재생해 1일차 석사 3명 연속, 2일차 석사 2명 연속으로
학력 묶음이 살아나는 것을 확인했다.

### ④ ③ 만들어진 시간표가 지금 상태에 종속되지 않던 문제

**증상** — ② 는 "면접 못 보는 지원자 2/83" 인데 ③ 은 "시간표에 들어간 사람
83/83" 이었다. ②·③ 이 서로 다른 말을 했다.

**원인 (두 가지)**
1. **시나리오 실행 탭**이 `/schedules/generate` 를 **짝 없이** 불렀다. 그러면
   전원 자유 배정(83/83)이 되고, 그 시간표가 같은 회차 목록의 **맨 위**에 올라간다.
   ③ 은 목록의 최신 것을 기본으로 보여 주므로, 부서 짝으로 만든 시간표 대신
   그 시간표를 보고 있었다.
2. ③ 에는 **지금 짝과 견주는 장치가 없었다.** 회차에 남아 있는 아무 시간표나
   '지금 상태' 처럼 보여 줬다.

**고침**
- `schedule_vs_pairs()` — 보고 있는 시간표를 지금 부서 짝과 견준다
  (짝이 바뀐 사람 · 짝도 없이 들어간 사람 · 짝은 있는데 못 들어간 사람).
  어긋나면 ③ 맨 위에 빨간 글씨로 "이 시간표는 지금 부서가 보낸 짝으로 만든 것이
  아닙니다 — 다시 만들어 주세요" 라고 말한다. 시간표 고르는 목록에도
  "⚠ 지금 부서 짝 N명과 다름" 을 붙인다.
- 시나리오 실행도 그 회차에 짝이 있으면 그 짝을 지켜 만든다.
- `handoff_pairs()` · `submit_team()` · `assign_team()` — **그 팀에 보낸 명단 안의
  사람** 과 **그 팀 담당자** 로 맺은 짝만 인정한다. 부서는 받은 목록에서 고르는
  것이지 목록 밖의 사람을 넣을 수 없다. 명단을 다시 보내 팀 구성이 바뀌면 옛
  짝은 여기서 걸러지고, 걸러진 건수는 ② 에 노란 글씨로 알린다 (조용히 사라지면
  숫자만 안 맞는다).

실측(R20260731-01, 짝 19명): 짝으로 만든 `532819bb` 는 배정 19/83 에 어긋남 0,
예전 `e3abcad6` 는 83/83 이지만 **짝이 바뀐 사람 17명 · 짝도 없이 들어간 사람
64명** 으로 잡힌다.

---

## 5. 다음에 할 만한 것

- 이번 순서 수정은 **다음번 3단계 '명단 보내기' 부터** 적용된다. 이미 보낸 회차의
  `data/handoff/*.json` 은 그대로이므로, 확인하려면 3단계를 다시 보내야 한다.
- `per_day > 6` 을 정말 지원하려면 스케줄러의 `HOURS` 계약을 회차마다 바꿀 수
  있게 해야 한다. 가용성(오전/오후 덩어리)·스키마 검증·rule_evaluator 가 전부
  물려 있어 큰 작업이다. 지금은 화면 경고로만 막아 두었다.
- 콘솔(`tools/test_console.py`)에는 자동 테스트가 없다. 순서·시각 계산
  (`order_for_interview` · `slot_labels` · `hour_clock` · `publish_handoff`)은
  순수 함수라 떼어내 테스트하기 좋다.

## 6. 이어서 할 때 첫 5분

```powershell
.\start_all.ps1          # 서비스 7개 + 콘솔 (코드 고칠 거면 --reload 로 떠 있는지 확인)
.\test_all.ps1           # 전체 테스트 (마지막 확인 시 139개 통과)
```

콘솔은 http://localhost:8501 . 스케줄러 테스트만 돌리려면
`cd services/04-scheduler; pytest -q`.
