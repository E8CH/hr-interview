[pytest]
testpaths = tests
addopts = -q --cov=app --cov-report=term-missing --cov-fail-under=70
filterwarnings =
    ignore::DeprecationWarning
markers =
    uses_version_manager: 01(version-manager) HTTP 호출 경로를 직접 검증하는 테스트
